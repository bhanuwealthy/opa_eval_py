[build-system]
requires = ["maturin>=1.5,<2.0"]
build-backend = "maturin"

[project]
name = "opa-eval"
version = "0.1.0"
description = "OPA policy evaluator using regorus"
requires-python = ">=3.8"

[tool.maturin]
features = ["pyo3/extension-module", "pyo3/abi3-py39"]
module-name = "opa_eval"
